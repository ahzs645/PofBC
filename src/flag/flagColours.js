// How the flag is painted.
//
// Its own three colours where they can be printed, or one ink with the letters where they cannot —
// which is how the logo appears in the reference, and what a fax, a stamp or an engraving gets.

export const FLAG_COLOURS = ['colour', 'ink']

export const FLAG_COLOUR_LABELS = {
  colour: 'Flag colours',
  ink: 'One ink'
}

export const FLAG_COLOUR_HINTS = {
  colour: 'The flag in its own navy, red and gold.',
  ink: 'The flag in the same colour as the letters.'
}
